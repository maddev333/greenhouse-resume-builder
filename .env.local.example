CosmosDB connection string for Azure-hosted database access with Cosmos SDK (azure-cosmos NPM package). Required for MVP data persistence layer in local dev & testing environments. Used to establish authenticated connections against the cloud-hosted resource using endpoint + primary-key authentication key from process.env COSMOS_ENDPOINT and COSMOS_AUTH_KEY system variables loaded at runtime by the Azure SDK client initialization function in api/db/cosmos-client.
