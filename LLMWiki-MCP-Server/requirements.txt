# Core: FastMCP only. Everything else (sqlite, hashing, fs polling, html
# parsing, markdown chunking) uses the Python standard library.
fastmcp>=3.0.0
# Phase 1: Azure AI Search backend (installed only when needed).
azure-search-documents>=11.6.0b1
# azure-identity pulled in via azure-search-documents dependency chain.
python-dotenv>=1.0.0

# Optional (PDF ingestion). Pure-Python, no system libraries.
# Uncomment if you intend to drop PDFs into data/corpus/.
# pypdf>=4.0.0

# Optional (Azure Document Intelligence — best-in-class OCR / layout for
# PDFs, images, and Office documents). Activate by setting
# LLMWIKI_DOC_INTEL_ENDPOINT. Auth via API key or DefaultAzureCredential.
# azure-ai-documentintelligence>=1.0.0
azure-identity>=1.17.0
